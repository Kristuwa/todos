interface Theme {
  color: {
    primaryColor: string;
    secondaryColor: string;
    bg: string;
    bgBtn: string;
    accentColor: string;
  };
}

export const theme: Theme = {
  color: {
    primaryColor: "#EBD8FF",
    secondaryColor: "#373737",
    bg: "#a1a1a1",
    bgBtn: "#EBD8FF",
    accentColor: "#5CD3A8",
  },
};
